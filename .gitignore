# Claude Code local settings (personal, not shared)
.claude/settings.local.json

# Local scratch markdown notes (not part of the published site)
extra-md-files/

# OS cruft
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/

# scripts/ (knowledge-graph extraction tooling) — deps + regeneratable output
scripts/node_modules/
scripts/output/

# Secrets (OpenAI API key etc.) — never commit
.env
scripts/.env

CLAUDE.md
progress.md
.vercel
